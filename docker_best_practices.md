(this document should eventually contain our best practices for how to create
 docker containers to be hosted on MetaBrainz infrastructure.)

Ideas have been collected here:

   https://tickets.metabrainz.org/browse/LB-402


Furhermore, we should address the following other topics:

* Running services (web server, data dumps, cron jobs, etc) as a non-root user. Likely <projectname> user.

